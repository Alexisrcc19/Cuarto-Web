<div class="container">
    <hr>
    <form method="POST" action="/test/tabla">
        <div class="form-group">
            <select name="nra" id="a" class="form-control">
                <% for(var i = 1; i < 13; i++) { %>
                <option  value="<%= i %>"><%= i %></option>
                <% } %>
            </select>
        </div>
        <div class="form-group">
            <select name="nrb" class="form-control">
                <% for(var i = 1; i < 13; i++) { %>
                <option  value="<%= i %>"><%= i %></option>
                <% } %>
            </select>
        </div>
        <button type="submit" class="btn btn-success">Calcular</button>
    </form>
</div>
