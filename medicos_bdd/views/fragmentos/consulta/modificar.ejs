<script src="/javascripts/utilidades.js"></script>

<script>
    $(document).ready(function () {
        verificar();
     cambiarFecha();            
        function cambiarFecha() {
            //alert(fecha);
            var d = new Date();
            //alert((d.getDate()+"/"+(d.getMonth()+1))+"/"+d.getFullYear());
            var mes= (d.getMonth()+1);
            var dia= d.getDate();
            var fecha = (d.getFullYear()+"-"+rellenarCeros(mes,2)+"-"+rellenarCeros(dia,2));
            console.log(fecha);
            $("#fecha").val(fecha);
        }
        function rellenarCeros(texto, nro_cero){
            texto= texto+"";
            if(texto.length < nro_cero){
                var aux= "";
                for(var i= texto.length ; i<nro_cero ; i++ ){
                    aux+="0";
                }
                return aux+texto;
            }else{
                return texto;
            }
        }
    });
</script>

<div class="container" style="margin-top: 20px;">    
    <div class="panel panel-primary">
        <div class="panel-heading">Modificar Consulta</div>
        <div class="panel-body">
            <form id="formulario" method="POST" action="/administracion/consulta/actualizar">
                <input type="hidden" name="external" value="<%= consulta.external_id %>"/>
                <div>
                    <ul class="nav nav-tabs" role="tablist">
                        <li role="presentation" class="active"><a href="#home" aria-controls="home" role="tab" data-toggle="tab">COnsulta Medica</a></li>
                        </ul>

                    <!-- Tab panes -->
                    <div class="tab-content">
                        <div role="tabpanel" class="tab-pane active" id="home">
                            <div class="form-group">
                                <label for="cedula" class="control-label">Diagnostico:</label>
                                <input type="text" class="form-control" id="diagnostico" name="diagnostico" value="<%= consulta.diagnostico %>" >
                            </div>
                            <div class="form-group">
                                <label for="apellidos" class="control-label">Motivo:</label>
                                <input type="text" class="form-control" id="motivo" name="motivo" value="<%= consulta.motivo %>">
                            </div>
                            <div class="form-group">
                                <label for="nombres" class="control-label">Receta:</label>
                                <input type="text" class="form-control" id="receta" name="receta" value="<%= consulta.receta %>">
                            </div>
                            
                            <div class="form-group">
                                <label for="fecha_nac" class="control-label">Fecha de nacimiento:</label>
                                <input type="date" class="form-control" id="fecha" name="fecha" value="<%= consulta.fecha %>" readonly> 
                            </div>
                            
                        </div>

                    </div>
                </div>
                <div class="modal-footer">                                    
                    <a href="/administracion/consulta" class="btn btn-default" >Cancelar</a>
                    <button type="submit" class="btn btn-primary">Guardar</button>
                </div>
            </form>
        </div>
    </div>
</div>
