<script src="/javascripts/utilidades.js"></script>
<script>
    $(document).ready(function () {
        verificar();
        cambiarFecha();
        function cambiarFecha() {
            var fecha = "<%= consulta.fecha %>";
            //alert(fecha);
            var d = new Date(fecha);
            //alert((d.getDate()+"/"+(d.getMonth()+1))+"/"+d.getFullYear());
            var mes = (d.getMonth() + 1);
            var dia = d.getDate();
            var fecha = (d.getFullYear() + "-" + rellenarCeros(mes, 2) + "-" + rellenarCeros(dia, 2));
            console.log(fecha);
            $("#fecha").val(fecha);
        }
        function rellenarCeros(texto, nro_cero) {
            texto = texto + "";
            if (texto.length < nro_cero) {
                var aux = "";
                for (var i = texto.length; i < nro_cero; i++) {
                    aux += "0";
                }
                return aux + texto;
            } else {
                return texto;
            }
        }
    });
</script>

<div class="container" style="margin-top: 20px;">    
    <div class="panel panel-primary">
        <div class="panel-heading">Modificar Consulta</div>
        <div class="panel-body">
            <form id="formulario" method="POST" action="/administracion/Historial_consulta/actualizar">
                <input type="hidden" name="id" value="<%= consulta.id %>"/>
                <div>
                    <ul class="nav nav-tabs" role="tablist">
                        <li role="presentation" class="active"><a href="#home" aria-controls="home" role="tab" data-toggle="tab">Datos de Consulta Medica</a></li>                                     
                    </ul>

                    <!-- Tab panes -->
                    <div class="tab-content">
                        <div role="tabpanel" class="tab-pane active" id="home">
                            <div class="form-group">
                                <label for="diagnostico">Diagnostico</label>
                                <textarea id="diagnostico" class="form-control" name="diagnostico"> <%= consulta.diagnostico %> </textarea>
                                <small id="diagnosticoHelp" class="form-text text-muted">Ingrese el diagnostico.</small>
                            </div>
                            <div class="form-group">
                                <label for="motivo">Motivo</label>
                                <textarea id="motivo" class="form-control" name="motivo"> <%= consulta.motivo %> </textarea>
                                <small id="motivoHelp" class="form-text text-muted">Ingrese el motivo de la consulta.</small>
                            </div>
                            <div class="form-group">
                                <label for="receta">Receta</label>
                                <textarea id="receta" class="form-control" name="receta"> <%= consulta.receta %> </textarea>
                                <small id="recetaHelp" class="form-text text-muted">Ingrese la receta.</small>
                            </div>
                            <div class="form-group">
                                <label for="fecha">Fecha</label>
                                <input type="date" class="form-control" id="fecha" name="fecha" aria-describedby="fechaHelp" value="<%= consulta.fecha %>" readonly>
                                <small id="fecha_nacHelp" class="form-text text-muted">Fecha de atencion.</small>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">                                    
                    <a href="/administracion/pacientes" class="btn btn-default" >Cancelar</a>
                    <button type="submit" class="btn btn-primary">Guardar</button>
                </div>
            </form>
        </div>
    </div>
</div>

