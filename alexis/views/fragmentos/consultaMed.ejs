
<script src="/javascripts/utilidades.js"></script>
<script>
     $(document).ready(function () {
        verificar();    
        cambiarFecha();            
        function cambiarFecha() {
            //alert(fecha);
            var d = new Date();
            //alert((d.getDate()+"/"+(d.getMonth()+1))+"/"+d.getFullYear());
            var mes= (d.getMonth()+1);
            var dia= d.getDate();
            var fecha = (d.getFullYear()+"-"+rellenarCeros(mes,2)+"-"+rellenarCeros(dia,2));
            console.log(fecha);
            $("#fecha").val(fecha);
        }
        function rellenarCeros(texto, nro_cero){
            texto= texto+"";
            if(texto.length < nro_cero){
                var aux= "";
                for(var i= texto.length ; i<nro_cero ; i++ ){
                    aux+="0";
                }
                return aux+texto;
            }else{
                return texto;
            }
        }
        });
</script>

<div class="container">    
    <form id="formulario" method="POST" action="/administracion/consulta/guardar" style="margin: 5%">
        <div>            
            <small class="label label-danger">Consulta Medica</small>            
        </div>
        <div>
            <input type="hidden" name="externalHistoria" id="external" value="<%= paci.historia.external_id %>">
            <input type="hidden" name="externalMedico" id="external" value="<%= external %>">
        </div>
        <div class="form-group">
            <label for="diagnostico">Diagnostico</label>
            <textarea id="diagnostico" class="form-control" name="diagnostico"></textarea>
            <small id="diagnosticoHelp" class="form-text text-muted">Ingrese el diagnostico.</small>
        </div>
        <div class="form-group">
            <label for="motivo">Motivo</label>
            <textarea id="motivo" class="form-control" name="motivo"></textarea>
            <small id="motivoHelp" class="form-text text-muted">Ingrese el motivo de la consulta.</small>
        </div>
        <div class="form-group">
            <label for="receta">Receta</label>
            <textarea id="receta" class="form-control" name="receta"></textarea>
            <small id="recetaHelp" class="form-text text-muted">Ingrese la receta.</small>
        </div>
        <div class="form-group">
            <label for="fecha">Fecha</label>
            <input type="date" class="form-control" id="fecha" name="fecha" aria-describedby="fechaHelp" readonly>
            <small id="fecha_nacHelp" class="form-text text-muted">Seleccione fecha de atencion.</small>
        </div>
        <button type="submit" class="btn btn-primary">Guardar</button>
        <a href="/administracion/pacientes" class="btn btn-danger">Volver</a>
    </form>
</div>
